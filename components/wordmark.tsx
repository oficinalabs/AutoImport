/** Marca tipográfica — decisão de design: sem logo, wordmark só. */
export function Wordmark() {
  return (
    <span className="font-display text-lg font-bold tracking-tight">
      <span className="font-medium">Auto</span>Import
    </span>
  );
}
